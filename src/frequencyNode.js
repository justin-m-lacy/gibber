export default class FrequencyNode {

	constructor(obj, count, left = null, right = null) {

		this.object = obj;
		this.count = count;

		this.left = left;
		this.right = right;

	} //

} // End FrequencyNode